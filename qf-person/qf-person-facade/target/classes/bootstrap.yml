spring:
  application:
    name: person
  cloud:
    config:
      enabled: true
      discovery:
        enabled: true
        service-id: CONFIG
      profile: ${person.config.profile:local}
eureka:
  client:
    service-url:
      defaultZone: ${person.eureka.cluster}