spring:
  application:
    name: some
  cloud:
    config:
      enabled: true
      discovery:
        enabled: true
        service-id: CONFIG
      profile: ${some.config.profile:dev}
eureka:
  instance:
    non-secure-port: ${some.port:9083}
  client:
    service-url:
      defaultZone: http://${eureka.host:localhost}:${eureka.port:9761}/eureka/