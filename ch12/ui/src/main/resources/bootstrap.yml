spring:
  application:
    name: ui

eureka:
  instance:
    non-secure-port: ${server.port:9080}
  client:
    service-url:
      defaultZone: http://${eureka.host:localhost}:${eureka.port:9761}/eureka/